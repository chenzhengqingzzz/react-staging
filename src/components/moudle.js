// //定义了一个React对象，给React对象追加了一个属性Component，值是一个类
// //这个文件做了两件事：用默认暴露的方式暴露了React这个对象，React这个对象身上有Component
// //用分别暴露的方式暴露了Component

// const React = {a: 1, b: 2}

// export class Component {}

// React.Component = Component


// export default React